"""Pytest configuration.

The presence of this file at the repository root causes pytest to add the
root directory to ``sys.path``, so tests can import the ``Leetcode`` package
(and its subpackages) regardless of where pytest is invoked from.
"""
